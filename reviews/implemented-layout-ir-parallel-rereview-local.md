## Review

- Correct: PyMuPDF invalid optional line bboxes now fail closed to `None` instead of constructing invalid `BoundingBox` values. `_make_bounding_box` coerces and rejects non-finite, negative, and inverted coordinates before returning a model (`packages/exeboard-ai/src/exeboard_ai/document_intelligence/parsing/adapters/pymupdf.py:159-174`); coverage includes negative/nan bboxes and a top-edge PDF parse path (`tests/integration/document_intelligence/test_pymupdf_parser.py:159-171`).
- Correct: Parser-port errors are modeled and PyMuPDF maps missing/unreadable, encrypted, and no-text cases to those errors (`packages/exeboard-ai/src/exeboard_ai/document_intelligence/parsing/ports.py:7-32`, `packages/exeboard-ai/src/exeboard_ai/document_intelligence/parsing/adapters/pymupdf.py:35-46`, `:73-77`, `:105-109`). Tests assert the hierarchy and adapter behavior (`tests/unit/document_intelligence/test_parser_ports.py:38-43`, `tests/integration/document_intelligence/test_pymupdf_parser.py:196-224`).
- Correct: Bounding-box/region strictness is in place for layout geometry: `BoundingBox` forbids extras, negatives, and non-finite values (`models.py:97-116`), `BoundingRegion` rejects zero-area boxes (`models.py:119-131`), and document layout validation bounds regions to known page dimensions (`models.py:698-712`). Tests cover zero area and page bounds (`tests/unit/document_intelligence/test_layout_models.py:215-288`).
- Correct: Table-cell provenance and table/figure detail linkage are enforced. Cells require source spans or bounding regions (`models.py:236-240`), tables/figures must link to matching wrapper blocks and wrapper IDs must have matching detail objects (`models.py:638-696`, `:538-549`). Tests cover missing cell provenance, table/figure ID mismatches, visual-only figures, and missing detail objects (`tests/unit/document_intelligence/test_layout_models.py:573-630`, `:679-744`).
- Correct: JSON round-trip for layout-bearing IR is covered by `model_dump`, `model_validate`, `model_dump_json`, and `model_validate_json` (`tests/unit/document_intelligence/test_layout_models.py:800-850`).

- Blocker: None found in the approved scope.

- Note (Low): Canonical `page_id`/`span_id` validation is improved but not fully canonical. `DocumentIR` parses IDs and checks document/page consistency (`models.py:465-477`), but `parse_page_id`/`parse_span_id` canonicalize only the returned document UUID (`core/ids.py:57-78`) and `DocumentIR` does not compare the stored strings to `make_page_id`/`make_span_id`. A `DocumentIR` without layout can therefore retain IDs with an uppercase UUID prefix such as `550E...:p0001`. If downstream code treats ID strings as canonical stable keys, add exact-string canonical checks or normalize nested IDs.
- Note: I could not run the pytest suite because pytest is not installed in the current environment (`pytest: command not found`; `.venv/bin/python -m pytest`: `No module named pytest`). I did run a small import-level smoke check for invalid bbox handling and zero-width `BoundingRegion`, which passed.

Final verdict: Approve for the current models + parser ports + PyMuPDF baseline scope, with the remaining canonical nested-ID normalization concern tracked as a low-severity follow-up.
