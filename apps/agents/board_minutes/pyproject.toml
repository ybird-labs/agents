[project]
name = "exeboard-agent-board-minutes"
version = "0.1.0"
description = "Board minutes agent application."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
exeboard-agent-board-minutes = "exeboard_agent_board_minutes.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
