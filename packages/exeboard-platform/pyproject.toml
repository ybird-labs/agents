[project]
name = "exeboard-platform"
version = "0.1.0"
description = "Platform adapters and runtime glue."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exeboard_platform"]
