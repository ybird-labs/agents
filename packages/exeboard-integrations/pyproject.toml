[project]
name = "exeboard-integrations"
version = "0.1.0"
description = "External service integrations."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exeboard_integrations"]
