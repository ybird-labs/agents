[project]
name = "exeboard-tools"
version = "0.1.0"
description = "Tool contracts and implementations."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exeboard_tools"]
