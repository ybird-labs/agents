[project]
name = "exeboard-temporal"
version = "0.1.0"
description = "Temporal workflows and activities."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exeboard_temporal"]
