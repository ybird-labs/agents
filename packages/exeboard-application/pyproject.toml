[project]
name = "exeboard-application"
version = "0.1.0"
description = "Application use cases and ports."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exeboard_application"]
